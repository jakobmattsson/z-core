{
  "name": "zee",
  "description": "Utility library for JavaScript promises",
  "version": "0.3.0",
  "author": "Jakob Mattsson <jakob.mattsson@gmail.com> (jakobmattsson.se)",
  "main": "./lib/index.js",
  "repository": {
    "type": "git",
    "url": "http://github.com/jakobmattsson/Z"
  },
  "scripts": {
    "coverage": "jscov src .cov && JSCOV=.cov mocha --reporter mocha-term-cov-reporter",
    "test": "mocha --grep \"$TESTS\"",
    "pretest": "coffee -co lib src",
    "prepublish": "coffee -co lib src"
  },
  "engines": {
    "npm": ">= 1.3 < 2",
    "node": ">= 0.10 < 0.11"
  },
  "devDependencies": {
    "jscov": "^0.5.7",
    "mocha": "^1.12.1",
    "mocha-term-cov-reporter": "^0.2.0",
    "chai-as-promised": "^4.1.0",
    "mocha-as-promised": "^2.0.0",
    "coffee-script": "^1.6.3"
  },
  "dependencies": {
    "q": "^1.0.0",
    "underscore": "^1.5.2"
  }
}
